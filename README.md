# Baccarat 百家樂遊戲系統

一個完整的百家樂遊戲系統，包含前台遊戲介面和後台管理系統。

## 功能特色

### 前台遊戲
- 🎴 完整百家樂遊戲邏輯（8副牌、補牌規則）
- 💰 6種下注選項：莊、閒、和、莊對、閒對、Super 6
- 📊 5種路單顯示：珠盤路、大路、大眼仔、小路、蟑螂路
- 🎨 高品質遊戲介面（深色主題、金色裝飾）
- 🌐 多語言支援（簡體中文、英文）

### 後台管理
- 👥 用戶管理（管理員、代理、會員三級架構）
- 💵 點數管理（入點、出點、現紅調整）
- 📈 交易記錄查詢
- 🔐 JWT 認證系統

## 技術棧

### 前端
- React 18 + TypeScript
- Tailwind CSS
- Framer Motion（動畫）
- Zustand（狀態管理）
- i18next（多語言）

### 後端
- Node.js + Express + TypeScript
- Prisma ORM
- PostgreSQL
- JWT 認證

## 快速開始

### 1. 安裝 PostgreSQL

使用 Docker：
```bash
cd /Users/justin/baccarat
docker compose up -d
```

或者安裝本地 PostgreSQL 並創建資料庫：
```sql
CREATE DATABASE baccarat;
```

### 2. 設定後端

```bash
cd backend

# 安裝依賴
npm install

# 生成 Prisma Client
npx prisma generate

# 執行資料庫遷移
npx prisma db push

# 創建初始資料（管理員、測試帳號）
npm run db:seed

# 啟動開發伺服器
npm run dev
```

後端將在 http://localhost:3001 運行

### 3. 設定前端

```bash
cd frontend

# 安裝依賴
npm install

# 啟動開發伺服器
npm run dev
```

前端將在 http://localhost:5173 運行

## 測試帳號

執行 `npm run db:seed` 後會創建以下測試帳號：

| 角色 | 帳號 | 密碼 | 初始餘額 |
|------|------|------|----------|
| 管理員 | admin | admin123 | 0 |
| 代理 | agent001 | agent123 | 100,000 |
| 會員 | member001 | member123 | 10,000 |

## API 端點

### 認證
- `POST /api/auth/login` - 登入
- `GET /api/auth/me` - 取得當前用戶資訊
- `POST /api/auth/change-password` - 修改密碼

### 用戶管理
- `GET /api/users` - 取得用戶列表
- `POST /api/users` - 創建用戶
- `PUT /api/users/:id` - 更新用戶
- `POST /api/users/:id/reset-password` - 重置密碼

### 交易
- `GET /api/transactions` - 取得交易記錄
- `POST /api/transactions` - 創建交易（入點/出點）
- `GET /api/transactions/balance/:userId` - 取得餘額

### 遊戲
- `GET /api/game/state` - 取得遊戲狀態
- `POST /api/game/play` - 下注並發牌
- `GET /api/game/history` - 取得遊戲歷史
- `GET /api/game/roadmap` - 取得路單資料

## 遊戲規則

### 點數計算
- A = 1 點
- 2-9 = 面值
- 10, J, Q, K = 0 點
- 總和取個位數

### 賠率
| 下注 | 賠率 |
|------|------|
| 莊 | 1:0.95 (抽5%水) |
| 閒 | 1:1 |
| 和 | 1:8 |
| 莊對/閒對 | 1:11 |
| Super 6 | 1:12 (兩張) / 1:20 (三張) |

## 專案結構

```
/baccarat
├── backend/
│   ├── src/
│   │   ├── controllers/    # API 控制器
│   │   ├── routes/         # 路由定義
│   │   ├── middleware/     # 中間件
│   │   └── utils/          # 工具函數
│   └── prisma/
│       └── schema.prisma   # 資料庫 Schema
│
├── frontend/
│   ├── src/
│   │   ├── components/     # React 元件
│   │   ├── pages/          # 頁面
│   │   ├── store/          # 狀態管理
│   │   ├── services/       # API 服務
│   │   └── i18n/           # 多語言
│   └── index.html
│
└── docker-compose.yml      # PostgreSQL 配置
```

## 授權

此專案僅供學習和娛樂用途，不涉及真實金流。
