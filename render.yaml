# Render Blueprint - https://render.com/docs/blueprint-spec

services:
  # Backend API Service
  - type: web
    name: baccarat-api
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install --include=dev && npm run build
    startCommand: npx prisma db push && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: postgresql://baccarat_db_24rf_user:9KXOIWX8BwMu13rQVFhjmIQbmWIjdmiw@dpg-d5ouijv5c7fs73aocvng-a/baccarat_db_24rf
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        value: https://baccarat-frontend.onrender.com,https://baccarat-admin.onrender.com

  # Frontend Static Site (Player)
  - type: web
    name: baccarat-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://baccarat-api.onrender.com/api
      - key: VITE_SOCKET_URL
        value: https://baccarat-api.onrender.com

  # Admin Dashboard Static Site
  - type: web
    name: baccarat-admin
    runtime: static
    rootDir: admin
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://baccarat-api.onrender.com/api
